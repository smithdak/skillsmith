---
description: Set up or tune the communication contract for this repo.
argument-hint: "[repo | global | both]"
---

Set up or retune the agent communication contract. Invoke the
`instructions-setup` skill and follow its workflow end to end: detect
harness surfaces with its script, interview for scope and modules, preview
the managed block, apply it, and verify.

Scope hint from the invocation (may be empty): $ARGUMENTS

Treat the hint as a preference, not consent — global writes still require an
explicit yes during the interview.
