{
  "should_trigger": [
    {
      "prompt": "load secrets from 1Password in my GitHub Actions workflow"
    },
    {
      "prompt": "stop pasting API keys into GitHub repo secrets, pull them from 1Password in CI"
    },
    {
      "prompt": "use a 1Password service account token in our deploy workflow"
    },
    {
      "prompt": "wire op:// references into GitHub Actions so CI resolves them at run time"
    },
    {
      "prompt": "set up 1password/load-secrets-action for this repo's pipeline"
    },
    {
      "prompt": "CI keeps needing new keys — wire GitHub Actions to 1Password so secrets resolve at run time"
    },
    {
      "prompt": "migrate the deploy workflow off stored secrets onto op:// references with a service account"
    },
    {
      "prompt": "every one of our deploy credentials is duplicated into the Actions UI and rotating them is a nightmare",
      "note": "situation phrasing: describes the circumstance without reusing the description's routing vocabulary — tests under-triggering, the axis lexical overlap hides"
    }
  ],
  "should_not_trigger": [
    {
      "prompt": "replace my local .env with op:// references and op run",
      "note": "local runtime injection, belongs to op-secrets"
    },
    {
      "prompt": "add a GitHub Actions secret with the value I'll paste",
      "note": "storing a plaintext secret, the opposite of this skill"
    },
    {
      "prompt": "write a wizard to walk me through creating a GitHub personal access token",
      "note": "interactive human setup, belongs to wizard"
    },
    {
      "prompt": "why is my GitHub Actions workflow failing on the test step?",
      "note": "CI debugging, unrelated to secret loading"
    },
    {
      "prompt": "rotate the OP_SERVICE_ACCOUNT_TOKEN now that someone left",
      "note": "rotation ops, not wiring CI to 1Password"
    },
    {
      "prompt": "set up op run for local development",
      "note": "local runtime injection — op-secrets"
    },
    {
      "prompt": "replace my .env secrets with 1Password op:// references",
      "note": "near-boundary: op-secrets's own trigger phrasing — op-github-secrets declares this out of scope, so it must not claim it"
    },
    {
      "prompt": "make me a setup wizard for Supabase — env keys, auth redirect URLs, the works",
      "note": "near-boundary: wizard's own trigger phrasing — op-github-secrets declares this out of scope, so it must not claim it"
    }
  ]
}
